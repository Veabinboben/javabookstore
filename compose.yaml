version: "3.8"
services:

  react:
    image: bookstorereact 
    build:
      context: ./bookstoreuireact
      dockerfile: Dockerfile
    container_name: bookstore-react
    ports:
      - "5173:8080"


  angular:
    image: bookstoreangular   
    build:
      context: ./bookstoreuiangular
      dockerfile: Dockerfile
    container_name: bookstore-angular
    ports:
      - "4200:8080"

  server:
    image: bookstoreserver  
    build:
      context: ./bookstoreserver
      dockerfile: Dockerfile
    container_name: bookstore-server
    ports:
      - "8080:8080"
    environment:
      - APP_FILE_UPLOAD_DIR=/app/uploads/
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/javadb
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=admin
      - SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=50MB
      - SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=50MB
      - SPRING_SERVLET_MULTIPART_ENABLED=true

    depends_on:
      - db
    volumes:
      - ./logs:/app/logs
      - uploads-data:/app/uploads/


  db:
    image: postgres:15
    container_name: bookstore-db
    environment:
      - POSTGRES_DB=javadb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro


volumes:
  db_data:
  uploads-data:
 